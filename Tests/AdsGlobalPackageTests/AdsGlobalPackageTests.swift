import XCTest
@testable import AdsGlobalPackage

final class AdsGlobalPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(AdsGlobalPackage().text, "Hello, World!")
    }
}
